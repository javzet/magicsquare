export type DefaultData = { position: number; value: number };
export type LevelType = 1 | 2 | 3;
export type MS_Base = Array<any> | Array<DefaultData>;
export type DataCell = { position: string; value: string };
